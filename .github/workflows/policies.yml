name: Terraform Policies CI/CD
on:
  workflow_dispatch:
  push:
    paths:
      - policies/**

permissions:
  id-token: write
  contents: read

jobs:
  provision-dev:
    name: "Provision development infrastructure"
    uses: ./.github/workflows/provision-infrastructure.yml
    with:
      folder-path: "policies"
      environment: "development"
    secrets: inherit

  provision-prod:
    name: "Provision production infrastructure"
    needs: provision-dev
    uses: ./.github/workflows/provision-infrastructure.yml
    with:
      folder-path: "policies"
      environment: "production"
    secrets: inherit
