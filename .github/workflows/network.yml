name: Terraform network CI/CD
on:
  workflow_dispatch:
  push:
    paths:
      - network/**

permissions:
  id-token: write
  contents: read

jobs:
  provision-dev:
    name: "Provision development infrastructure"
    uses: ./.github/workflows/provision-infrastructure.yml
    with:
      folder-path: "network"
      environment: "development"
    secrets: inherit

  provision-prod:
    name: "Provision production infrastructure"
    needs: provision-dev
    uses: ./.github/workflows/provision-infrastructure.yml
    with:
      folder-path: "network"
      environment: "production"
    secrets: inherit
